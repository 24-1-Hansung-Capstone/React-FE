import RelatedSearchWords from "../RelatedSearchWords";

const style = {
    relatedSearchWords :{
        marginLeft: "120px",
        display : "flex",
        flexDirection: "row",
        flexWrap: "wrap",
    }
};

export default style;