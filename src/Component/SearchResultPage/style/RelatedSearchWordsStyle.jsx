import RelatedSearchWords from "../RelatedSearchWords";

const style = {
    relatedSearchWords :{
        display : "flex",
        flexDirection: "row",
        flexWrap: "wrap",
    }
};

export default style;