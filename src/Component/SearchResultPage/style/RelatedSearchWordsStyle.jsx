import RelatedSearchWords from "../RelatedSearchWords";

const style = {
    relatedSearchWords :{
        display : "flex",
        flexDirection: "row",
        flexWrap: "wrap",
        marginBottom : "10px"
    }
};

export default style;